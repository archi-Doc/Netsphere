# コードレビューと修正記録（2026-09-09）

ソリューション構成と既存テストを確認し、パケットの受信・応答照合、Transmissionの開始・完了・破棄、Relayの認証と共有状態、RPCの生成コードを重点的に調査した。既存189件のテストは修正前にも成功していたが、以下の境界条件と競合には追加の修正が必要だった。

| 対象 | 確認した問題 | 修正内容 |
| --- | --- | --- |
| Burst受信 | 先頭Geneの再送に対して、未完了のBurstにも全体完了と解釈されるACKを返す | 重複Geneを受信状態に応じて処理し、完了前のACKを抑止 |
| ACK管理 | Burst用の共有マーカーに重複Gene番号が蓄積する | マーカーを通常のACKキューとして更新しない |
| RPC開始 | 応答用Transmissionの登録より先に送信を開始する | Block、RPC、コールバック、Stream要求で受信側を先に登録 |
| 待機終了 | タイムアウト・キャンセルと応答到着が競合すると受信バッファを回収できない | 放棄した待機の遅延応答を回収し、キャンセルトークンを伝播 |
| Transmission破棄 | 初期化と破棄が競合して、破棄後に受信状態へ戻る | 初期化を内部ロックで保護し、Initial状態からのみ遷移 |
| コールバック | 非再入のコレクションロックを保持した破棄中にユーザー処理を呼び出す | 破棄通知を切り離して非同期に実行し、同じ接続への再入を可能にする |
| 状態の公開 | ポーリングされるMode・受信位置が通常フィールド | volatileによってスレッド間の可視性を保証 |
| Packet応答 | PacketIdだけで保留中の要求を完了させる | 応答種別を照合し、直接送信では送信先Endpointも照合 |
| Packet再送 | 送信キューが参照しているバッファのID・チェックサムを書き換える | 再送時の変更前に独立したバッファへコピー |
| Packet終了 | 通常のタイムアウト戻り値では保留キューを解放しない | finallyで保留項目を削除し、再送上限でも待機を完了 |
| Relay拒否 | 送信元不一致・RelayPoint枯渇・外側復号失敗の後も処理を継続する | 拒否時は直ちに終了し、未認証データを転送しない |
| Relay共有状態 | IPv4/IPv6受信、設定変更、清掃でExchangeやEndpointキャッシュに競合する | 非再入ロックの取得順を整理して共有状態を同期 |
| Relay境界 | IDの0・内外ID重複、暗号化長、最小整数の符号反転、ポイント加算のオーバーフロー | 入力検証、重複回避、範囲確認、飽和演算を追加 |
| Relay鍵 | 呼び出し元の鍵配列と共有する。清掃後にキャッシュが古いまま残る | Exchangeの鍵をコピーし、清掃後にRelayKeyを再構築して公開 |
| Generatorの解放処理 | 例外やデシリアライズ失敗時に要求・応答バッファを返却しない | 所有権移譲を明示し、finallyで返却 |
| GeneratorのMemory処理 | 構築済みMemory型を正しく分類できない。借用バッファを返却してから応答に利用する | 型判定を修正し、借用データのコピーを返却前に実施。通常のMemory戻り値には独立した配列を返す |
| Generatorの引数 | CancellationTokenを除外した後の単一引数にItem1を参照する。単独ResponseChannelのrefと戻り値参照が不正 | 実際にシリアライズする引数数でコードを生成 |
| Generatorのフィルター | staticインスタンス共有、クラスフィルター未初期化、ResponseChannel経路でのフィルター迂回 | 呼び出しごとの初期化、クラス・メソッド両方の適用、共通の実行経路へ統合 |
| NetResult送信 | 1バイトのenumをUnsafe.Asで8バイトとして読む | enumとして読み、ulongに数値変換 |

追加テストは `xUnitTest/Tests/ProtocolReviewTest.cs`、生成コードの検証用サービスは `xUnitTest/Services/IReviewService.cs` にまとめた。生成器のテンプレートに加え、実際に出力されたFrontend／BackendのC#も確認した。生成ファイル自体はobj配下の検証用出力であり、変更の正本はNetsphereGeneratorにある。

検証結果：

- ソリューション全体のDebug／Releaseビルド：それぞれ警告0、エラー0。
- Debug／Releaseの全テスト：それぞれ219件成功、失敗0、スキップ0。
- 新規回帰ケース：30件。重複・異常応答の注入、プール参照数、キャンセル、並列実行、実際のRPCと既存の複数段Relay通信を含む。
- `git diff --check`：問題なし。

適用時にはクライアントとサーバーの両方を再ビルドすること。特にMemory型の応答は生成器の型判定修正で転送表現が変わるため、旧生成コードとの混在互換性は保証しない。自動生成するフィルターは呼び出しごとに生成するが、DIから取得するフィルターの寿命はDI登録に従う。

検証はローカル環境で実施した。長時間の実ネットワーク負荷試験、すべてのパケット損失・順序逆転・切断タイミングの組み合わせ、旧バージョンとの相互運用試験までは実施していない。
